import { checkUser } from 'coffee-reviewer/helpers/check-user';
import { module, test } from 'qunit';

module('Unit | Helper | check user');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = checkUser([42]);
  assert.ok(result);
});
