import { pagination } from 'coffee-reviewer/helpers/pagination';
import { module, test } from 'qunit';

module('Unit | Helper | pagination');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = pagination([42]);
  assert.ok(result);
});
